# Compose, DataStore, and Kotlin metadata are handled by their consumer rules.
# Keep BuildConfig readable for diagnostics in release crash reports.
-keep class com.pramod.chessmasteroffline.BuildConfig { *; }
